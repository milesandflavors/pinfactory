# NEVER commit secrets — these go into GitHub Secrets, not the repo
secrets.json
tokens.json

# Raw source photos (large, not needed in the cloud — cloud posts the finished done/ images)
input_images/

# Local backup of already-posted pins (large, not needed in the cloud)
done_archive/

# Video pins (large, posted manually — cloud_post.py has no video upload yet)
done/*.mp4
done/*.mov
done/*.webm

# Python junk
__pycache__/
*.pyc

# staging + preview artifacts (not for the repo)
_sort/
*_preview.png
_contact.png

# Scratch/working exports (2026-09-10: accidentally swept into a commit via `git add -A` — never repo material)
all_meta.json
all_used_slugs.txt
never_used_meta.json
never_used_slugs.txt
examples_out.txt
remaining21.txt
intros.json
pins_export.xlsx
pinterest_calendar_artifact.html
infographic_pack_extracted/
infographic_source_pack/
infographic_source_pack.zip
